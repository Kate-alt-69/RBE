name: Finalize FLD-004 Multi-Source FieldManager

on:
  push:
    branches:
      - ci/fld-004-multisource-20260924
    paths:
      - ".github/patches/fld-004-multisource.py"
      - ".github/workflows/fld-004-finalize.yml"

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ci/fld-004-multisource-20260924
          fetch-depth: 2

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: engine

      - name: Apply FLD-004
        run: python .github/patches/fld-004-multisource.py

      - name: Format and check route-engine
        working-directory: engine
        run: |
          cargo fmt --all
          cargo fmt --all -- --check
          cargo check -p route-engine

      - name: Clippy workspace
        working-directory: engine
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test route-engine
        working-directory: engine
        run: cargo test -p route-engine

      - name: Commit verified FLD-004 source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Kate"
          git config user.email "dhwanksh2000@gmail.com"
          git add engine/crates/route-engine/src/ast.rs \
                  engine/crates/route-engine/src/parser.rs \
                  engine/crates/route-engine/src/field_manager.rs \
                  doc/field-manager.md
          git diff --cached --quiet && { echo "FLD-004 produced no source diff"; exit 1; }
          git commit -m "FEAT | FLD-004 add multi-source FieldManager bindings"
          git push origin HEAD:ci/fld-004-multisource-20260924
