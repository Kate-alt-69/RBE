name: Finalize FLD-025 Forwarded Header Lines

on:
  push:
    branches:
      - ci/fld-025-forwarded-lines-20260926
    paths:
      - .github/patches/fld-025-forwarded-lines.py
      - .github/workflows/fld-025-finalize.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: engine

      - name: Apply FLD-025
        run: python .github/patches/fld-025-forwarded-lines.py

      - name: Format and check route-engine
        working-directory: engine
        run: |
          cargo fmt --all
          cargo fmt --all -- --check
          cargo check -p route-engine

      - name: Clippy route-engine library
        working-directory: engine
        run: cargo clippy -p route-engine --lib -- -D warnings

      - name: Test route-engine
        working-directory: engine
        run: cargo test -p route-engine

      - name: Commit verified FLD-025 source
        run: |
          set -euo pipefail
          git config user.name "Kate"
          git config user.email "dhwanksh2000@gmail.com"
          git add doc/x.route/README.md engine/crates/route-engine/src/discovery.rs
          git diff --cached --quiet && { echo "no FLD-025 source changes"; exit 1; }
          git commit -m "FIX | FLD-025 consume all trusted forwarding lines"
          git push origin HEAD:ci/fld-025-forwarded-lines-20260926
