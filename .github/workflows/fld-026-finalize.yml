name: Finalize FLD-026 Trusted Proxy Protocol

on:
  push:
    branches:
      - ci/fld-026-proxy-protocol-20260926
    paths:
      - .github/patches/fld-026-proxy-protocol.py
      - .github/workflows/fld-026-finalize.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: engine

      - name: Apply FLD-026
        run: python .github/patches/fld-026-proxy-protocol.py

      - name: Format and check route-engine
        working-directory: engine
        run: |
          cargo fmt --all
          cargo fmt --all -- --check
          cargo check -p route-engine

      - name: Clippy route-engine library
        working-directory: engine
        run: cargo clippy -p route-engine --lib -- -D warnings

      - name: Test route-engine
        working-directory: engine
        run: cargo test -p route-engine

      - name: Commit verified FLD-026 source
        run: |
          set -euo pipefail
          git config user.name "Kate"
          git config user.email "dhwanksh2000@gmail.com"
          git add doc/x.route/README.md engine/crates/route-engine/src/discovery.rs
          git diff --cached --quiet && { echo "no FLD-026 source changes"; exit 1; }
          git commit -m "FIX | FLD-026 reject invalid trusted proxy protocols"
          git push origin HEAD:ci/fld-026-proxy-protocol-20260926
