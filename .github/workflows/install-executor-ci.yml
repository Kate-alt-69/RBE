name: Install Executor CI

on:
  push:
    paths:
      - 'install-executor/**'
      - '.github/workflows/install-executor-ci.yml'
  pull_request:
    paths:
      - 'install-executor/**'
      - '.github/workflows/install-executor-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  install-executor:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: install-executor
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: install-executor
      - name: fmt check
        run: cargo fmt -- --check
      - name: clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: test
        run: cargo test
