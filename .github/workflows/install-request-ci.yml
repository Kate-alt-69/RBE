name: Install Request CI

on:
  push:
    paths:
      - 'install-request/**'
      - '.github/workflows/install-request-ci.yml'
  pull_request:
    paths:
      - 'install-request/**'
      - '.github/workflows/install-request-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  install-request:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: install-request
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: install-request
      - name: fmt check
        run: cargo fmt -- --check
      - name: clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: test
        run: cargo test
