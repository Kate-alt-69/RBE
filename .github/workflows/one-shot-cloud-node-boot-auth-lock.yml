name: One-shot Cloud Node boot auth lock

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-shot-cloud-node-boot-auth-lock.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: one-shot-cloud-node-boot-auth-lock
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by the Cloud Node boot recovery gate validation.'
