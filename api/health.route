:import[net]

class Route {
    async get(req) {
        const health = net.health();
        const engine = net.ping();

        return {
            ok: true,
            status: health.status,
            uptime: health.uptime,
            engine: engine
        };
    }
}
