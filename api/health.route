:import[net]

class Route {
    async get(req) {
        const engine = net.ping();
        return {
            ok: true,
            status: "ok",
            security: true,
            engine: engine
        };
    }
}
