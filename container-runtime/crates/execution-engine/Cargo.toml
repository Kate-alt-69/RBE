[package]
name = "execution-engine"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
anyhow = "1"
# Security-reviewed 45.x baseline. 45.0.2 fixes the WASIp1 fd_renumber
# resource leak; 45.0.3 additionally fixes the WASI hard-link/rename
# permission bypass disclosed in June 2026.
wasmtime = "45.0.3"
