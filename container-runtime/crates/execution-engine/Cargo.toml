[package]
name = "execution-engine"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
anyhow = "1"
# Wasmtime 45.0.0 is the current supported stable release on the branch's
# security review date. The older 36.x line had later security fixes applied
# in 36.0.10 and subsequent releases.
wasmtime = "45.0.0"
