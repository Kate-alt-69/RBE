//! Phase 2 stub (§12, §5.1). Empty on purpose — this crate's job in
//! the layered chain (sandbox-primitives -> resource-limits ->
//! execution-engine -> ipc-protocol -> container-runtime-core ->
//! container-bin) is described in rust-migration-plan.md §5.1; nothing
//! here yet.
