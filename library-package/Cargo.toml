[package]
name = "rbe-library-package"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"
zip = { version = "2.4", default-features = false, features = ["deflate"] }
